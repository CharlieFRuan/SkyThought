[project]
name = "skythought"
version = "0.1.0"
description = "Skythought Evals: Evaluation and Data Generation Tools for Reasoning Models"
authors = [
    { name = "NovaSky Team" }
]
requires-python = ">=3.9,<3.13"
dependencies = [
    "vllm==0.9.2",  # 0.8.5 caused issue with TP=2 on some machiens, likely torch and NCCL related
    "transformers==4.53.2",  # 4.54.0 may lead to error `ValueError: 'aimv2' is already used by a Transformers config, pick another name.`
    "word2number",
    "scipy",
    "datasets",
    "latex2sympy2",
    "pydantic",
    "setuptools",
    "typer",
    "hf_transfer",
]
license = { text = "Apache-2.0" }
readme = "README.md"

[project.urls]
Repository = "https://github.com/NovaSky-AI/SkyThought"

[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
include-package-data = true  # Ensures package_data is included in wheel

[tool.setuptools.packages.find]
where = ["."]
include = [
    "skythought",          # top-level package
    "skythought.evals*"    # subpackage(s) under skythought/evals
]

[tool.setuptools.package-data]
skythought = ["evals/**/*.yaml", "evals/**/*.yml"]

[project.scripts]
skythought = "skythought.evals.cli:main"

[project.optional-dependencies]
dev = ["pytest", "pytest-mock", "pytest-asyncio", "black", "ruff", "pre-commit"]

[tool.ruff]
line-length = 160

[tool.ruff.lint]
extend-select = ["E", "F", "I", "ASYNC", "B"]
ignore = ["F811", "B006"]
